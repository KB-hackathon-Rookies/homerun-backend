# HomeRun LLM Context

이 파일은 팀 루키즈 구성원이 Claude, Codex, Cursor, Gemini 등 LLM 도구로 작업을 시작할 때 공통으로 읽히기 위한 기준 문서다.

작업을 시작할 때는 먼저 이 파일과 현재 저장소의 `README.md`를 읽는다. 이 문서, Notion, 스프레드시트, README, 실제 코드 및 설정이 서로 충돌하는 경우 다음 우선순위를 따른다.

1. 사용자가 현재 작업에서 직접 전달한 지시
2. 실제 코드와 설정
3. 요구사항 정의 **시트2** (기능ID의 단일 출처)
4. `config_effective` 테이블 (기준 수치의 단일 출처. 시드는 `V2` 와 이후 마이그레이션에 나뉘어 있다)
5. 최신 `README.md`
6. `LLM_CONTEXT.md`
7. Notion 및 기타 문서

이 프로젝트는 문서가 여러 벌 존재하고 서로 어긋나 있다. 충돌을 발견하면 임의로 판단해 숨기지 말고, 어떤 내용이 충돌했으며 어떤 기준을 우선 적용했는지 사용자에게 알린다. 현재 알려진 충돌은 **문서 간 모순** 장에 정리해 두었다.

---

# 테스트 컨벤션

## 기본 원칙

* 새로운 기능(feature)을 추가할 때는 반드시 해당 기능의 단위테스트를 함께 작성한다.
* 테스트 프레임워크는 JUnit 5를 사용한다. 필요 시 Mockito를 함께 사용한다.
* **테스트는 H2가 아니라 진짜 PostgreSQL에서 돈다.** Redis 기능은 Redis Testcontainer로 검증한다. Testcontainers가 빈 저장소를 띄우고 Flyway 마이그레이션을 처음부터 적용한다.
* 그래서 테스트를 돌리려면 Docker가 실행 중이어야 한다.
* `feat` 커밋과 `test` 커밋은 같은 브랜치/PR 안에서 연속된 쌍(pair)으로 존재해야 한다.
* 기능 구현 커밋과 테스트 작성 커밋은 하나의 커밋으로 묶지 않고 분리한다.

## 커밋 순서와 메시지

```text
1) ✨ Feat: 기능명 구현
2) ✅ Test: 기능명 단위테스트 추가
```

예시:

```text
✨ Feat: 정책 자격 3단계 판정 API 구현
✅ Test: 정책 자격 3단계 판정 단위테스트 추가
```

## 테스트 코드 작성 규칙

* 테스트 클래스명은 `{대상클래스명}Test` 형식을 사용한다. 예: `PolicyRuleEngineTest`
* 테스트 메서드명은 `should_기대결과_when_조건` 형식을 사용하거나 한글 `@DisplayName`을 함께 작성한다.
* 하나의 테스트 메서드는 하나의 검증 시나리오만 담당하며 Given-When-Then 구조를 권장한다.
* 외부 연동(공공데이터 API, 오픈뱅킹, LLM Gateway)은 Mockito로 mocking하여 격리 테스트한다.
* **양쪽이 같은 곳에서 나온 값을 비교하는 테스트는 아무것도 검증하지 않는다.** 시드가 맞는지 보려면 응답끼리 비교하지 말고 시드를 직접 읽는다. 통과하는 것을 확인했다고 검증한 것이 아니다 — **일부러 틀리게 만들어 실패하는지도 확인한다.**

## 규칙 엔진 테스트는 특히 중요하다

* 정책 판정은 **같은 규칙 버전 + 같은 입력이면 항상 같은 결과**여야 한다(`NFR-01-01`).
* 판정 로직에는 반드시 경계값 테스트를 붙인다. 연령 상한, 소득 컷오프, 보증금 한도는 전부 `<` 와 `<=` 를 틀리기 쉽다.
* 기준 수치는 테스트에도 하드코딩하지 않고 `config_effective` 픽스처에서 읽는다.

## PR 규칙

* `feat` 커밋만 있고 대응하는 `test` 커밋이 없는 경우 PR을 머지할 수 없다.
* 리뷰어는 PR에서 `feat`/`test` 커밋 쌍의 존재 여부를 체크리스트로 확인한다.

## 테스트 실패 후 수정 커밋

* **Push 이전** — 로컬에서 실패를 발견하면 별도 커밋을 남기지 않는다. 고치고 통과한 상태로 `feat`/`test` 커밋을 만든다.
* **Push 이후** — 리뷰나 CI 에서 의미 있는 실패가 나오면 `🚨 Fix` 커밋을 더한다. 사소한 것은 push 전에 정리한다.

---


# 프로젝트 개요

* **프로젝트명:** 홈런(HomeRun)
* **팀:** KB IT's Your Life, 팀 루키즈
* **본선 일정:** 2026.09.08 ~ 2026.09.10
* **서비스:** 사회초년생의 첫 독립에 필요한 자금·주거·정책·금융을 하나의 경로로 연결해 현실적인 독립 계획을 설계하는 서비스
* **핵심 사용자:** 부모 집에서 나와 처음 자기 집을 구하는 청년
* **북극성 지표:** **완주율** = 실제 신청·수급 도달 인원 ÷ 자격 확인을 시작한 인원

## 핵심 목표

* 흩어진 정책·대출·서류 정보를 개인별 **실행 순서** 하나로 묶는다.
* 판정 결과에 항상 공식 근거를 붙여 "왜 되는지/왜 안 되는지"를 설명한다.
* 계약 전 매물 검증과 순서 함정 경보로 되돌릴 수 없는 실수를 막는다.
* 입주 이후 현금흐름·상환·정책 유지까지 이어서 관리한다.

## 야구 비유 — UI 전반의 뼈대

| 단계 | 하는 일 | 기능ID 접두사 |
|---|---|---|
| 벤치 | 로그인, 온보딩, 약관, 진행상태 복원 | `COM` |
| 1루 | 내 정보 입력 → 독립 가능성 진단 | `DIA` |
| 2루 | 정책 자격 판정, 대출·월세 비교, 대안 경로 | `POL` `GTE` `AST` `ALT` `WRN` `PRE` |
| 3루 | 서류 준비, 매물 검증, 계약, 신청 순서 | `VER` `EVI` `ISS` `PLC` `PRP` `FAM` `SEQ` `APP` |
| 홈 | 입주 후 현금흐름·상환·정책유지·금융교육 | `RF-HM` `RF-CR` `RF-AS` `RF-MV` |

문서에 따라 홈을 **4루**라고도 쓴다(플로우 정의, 시트5). 같은 단계다.

---

# 저장소

## 원격 저장소

* **백엔드:** `https://github.com/KB-hackathon-Rookies/homerun-backend.git`
* **프론트엔드:** `https://github.com/KB-hackathon-Rookies/homerun-frontend.git`

통합 저장소는 없다. 이 문서는 백엔드 저장소 루트에 있고, 프론트엔드 작업 시에도 여기를 읽는다.

## 로컬 기준 경로

두 저장소를 같은 상위 디렉터리에 나란히 둔다.

```text
<작업 폴더>/
  ├─ homerun-backend/
  └─ homerun-frontend/
```

저장소 경로를 코드나 설정에 하드코딩하지 않는다. 사람마다 다르다.


## 현재 구현 상태

| | |
|---|---|
| 도메인 패키지 | 20개 (`alternative` `application` `asset` `auth` `consent` `contract` `dashboard` `diagnosis` `document` `fact` `house` `member` `openbanking` `plan` `policy` `property` `region` `rent` `terms` `verification`) |
| 컨트롤러 | 34개 |
| 마이그레이션 | `V1` ~ `V36` |
| 테스트 | 84개 클래스 |
| 팩트 레지스트리 | 210건 (`V2` 가 141건, 이후 마이그레이션이 69건) |

**이제 "기존 코드를 따른다"가 실제로 적용된다.** 새 구조를 지어내기 전에 같은 일을 하는 도메인을 먼저 찾는다.

구현된 API 묶음.

```text
/api/v1/auth · /auth/email                 로그인·토큰·소셜·이메일 인증 가입
/api/v1/members/me                         회원 정보·탈퇴
/api/v1/plans                              계획·단계·입력
/api/v1/plans/{planId}/input               계획 입력 이력·오픈뱅킹 동기화·프로필 프리필
/api/v1/plans/{planId}/diagnosis           1루 진단 계산·시뮬레이션 (DIA-02)
/api/v1/plans/{planId}/policies/jeonse     전세 정책·반환보증·보증료 지원 판정
/api/v1/plans/{planId}/policies/youth-savings  청년미래적금 판정
/api/v1/plans/{planId}/alternatives        재도전 큐·미충족 원인·대안 재계산 (ALT-01)
/api/v1/plans/{planId}/assets              IRP·청약·적금 비교 (AST-01)
/api/v1/plans/{planId}/verifications       추가 확인 조건 조회·재판정
/api/v1/plans/{planId}/properties          매물 후보·검증
/api/v1/plans/{planId}/contract            계약 실행
/api/v1/plans/{planId}/consents            가구원 동의
/api/v1/plans/{planId}/applications        정책 신청
/api/v1/plans/{planId}/documents           서류 보유·유효기간
/api/v1/plans/{planId}/tasks               할 일·대시보드
/api/v1/policies/rent                      월세 정책 판정
/api/v1/guarantee-agencies                 보증기관 비교 (POL-03-08)
/api/v1/bank-loan-rates                    은행별 공시 평균금리 비교 (POL-03-07)
/api/v1/documents · /contract-checklist    서류 카탈로그·발급 안내·계약 체크리스트
/api/v1/consents/{token}                   가구원 공개 링크
/api/v1/houses · /buildings · /addresses · /regions  주택·건축물대장·주소·지역
/api/v1/real-estate/rent-transactions      실거래가
/api/v1/open-banking                       금융 요약·스냅샷
```

---


# 주요 기능 도메인

요구사항 322건, 65개 블록. 우선순위는 높음 185 / 중간 122 / 낮음 15.

## 공통 · 벤치 (`COM`, 47건)

* 카카오 소셜 로그인(OAuth2), 이메일 로그인, 자동 로그인 유지
* 진행 상태 저장·복원, 단계 이동 가능 여부 판정, 계획 초기화
* 입력 자동 저장, **모름 입력 허용**, 필수값·범위 검증
* 약관·개인정보 동의 및 동의 이력, 알림 설정
* 데이터 기준일 표시, 공식 출처 연결
* 독립 도구 — 계약 체크리스트, 부동산 용어사전, 전세사기 예방정보

## 1루 진단 (`DIA`, 23건)

* 기본정보·병역·가구·고용소득·자산부채·주거조건 입력
* 오픈뱅킹 연동 및 목 데이터 지원(`DIA-01-00`)
* 초기 필요자금, 월 현금흐름, 부족자금 산출
* 정책 적용 전후 비교, 독립 가능성 판정
* **RIR은 계산하지 않는다.** `DIA-02-03`(RIR 계산)과 `DIA-02-07`(RIR 판정 기준 적용)은 범위에서 뺐다 — **RIR 제외 결정** 참고

## 2루 정책 (`POL` `GTE` `AST` `ALT` `WRN` `PRE`, 52건)

* 3단계 자격 판정, 판정 근거 제공, 가구 기준 이중 심사, 자격 소멸 시계
* 전세대출 10건 — 사람·집 조건 분리 판정, 청년/일반 버팀목, 서울시 이자지원, 기금·일반 구분
* 월세 9건 — 보증부월세, 주거안정, 주거급여, 세액공제, 중복 판정
* 반전세 6건 — 전월세전환율, 조정 시뮬레이션
* 반환보증 4건, 자산(IRP·청약) 6건, 대안 경로 5건, 경고 2건, 준비도 3건

## 3루 실행 (`VER` `EVI` `ISS` `PLC` `PRP` `FAM` `SEQ` `APP`, 62건)

* 추가 확인 조건 조회·입력·재판정
* 증빙 관리 — 정책-서류 관계, 중복서류 통합, 유효성, 서류 가치 표시
* 발급 안내 및 주변 발급처 검색
* 매물 검증 8건 — 등기부, 건축물대장, 공시가격, 신탁, 전세가율, 다가구, 소액임차인, 체납
* 계약 실행 7건, 가구원 확인 5건
* Step DAG 생성, 비가역 순서 경고, 착수 데드라인 역산
* 신청 체크리스트, 준비 완료 판정, 공식 신청 연결, 결과 기록

## 홈 정착 (`RF-HM` `RF-CR` `RF-AS` `RF-MV`, 100건)

* `RF-HM` 28건 — 대시보드, 계획 대비 실제 현금흐름, 정책 수급, 대출 상환, 연체 위험, 재진단, 월간 체크인
* `RF-CR` 40건 — 신용 진단, 기초·대출·연체·부채·사기 교육, 체험형 교육, 이수 관리, 상담 연계, 사후관리
* `RF-AS` 11건 — 자산형성 준비도, 상품 매칭, 실행과 추적
* `RF-MV` 21건 — 입주 당일 행정, 입주 직후 신청, 30일, 전세·월세 사후관리

## AI · 운영 · 비기능 (`AI` `ADM` `SEC` `NFR` `KPI` `OUT`, 38건)

* AI 코치 10건, 정책 운영 8건, 개인정보 보호 5건, 신뢰성·품질 6건, KPI 5건, 범위 제외 4건

---

# 기술 스택

버전은 `build.gradle` 과 `package.json` 이 단일 출처다. 여기에는 **알고 있어야 동작이 이해되는 것만** 둔다.

## Backend

* Spring Boot 4.1.1 / Java 17 / PostgreSQL 17
* **`ddl-auto: validate`** — 엔티티가 스키마를 만들지 않는다. 스키마는 Flyway 만 바꾼다.
* **테스트는 진짜 PostgreSQL 에서 돈다** (Testcontainers). H2 를 넣지 않는다 — Postgres 전용 문법이 검증되지 않는다.
* **Redis** 는 이메일 인증번호와 단기 인증 상태에 실제로 쓴다.
* spotless + palantirJavaFormat, 4칸 들여쓰기. **포맷터가 코드를 다시 접으므로** 문자열 치환으로 코드를 고칠 때는 포맷 후 텍스트를 다시 확인한다.
* `spring-boot-docker-compose` 가 개발 시 PostgreSQL 을 자동 기동한다.
* Spring Data JPA. MyBatis 와 Mapper XML 은 없다.

## Frontend

Nuxt 4 + Vue 3 `<script setup>` + Tailwind 4 + pnpm. 자세한 것은 `homerun-frontend` 저장소를 본다.

Tailwind v4 는 `postcss.config` 도 `tailwind.config.js` 도 쓰지 않는다. Vite 플러그인과 `main.css` 의 `@import "tailwindcss"` 가 전부다.

## Infra / External

* Docker Compose (`compose.yaml` 로컬, `compose.prod.yaml` 앱 포함)
* GitHub Actions — `ci.yml`, `cd.yml`. **배포 대상 서버 미정** (`deploy` 잡이 비어 있다)
* 오픈뱅킹 auth API — 계좌·소득 조회
* 공공데이터포털 / 보조금24 / 복지로 — 정책 원문 수집
* 정부24, 인터넷등기소, 홈택스, 위택스 — 서류 발급 경로
* **온통청년 API 는 사용 불가로 확정**(폐기, 404). `FCT-134`
* 지도 API(카카오맵·네이버지도) — 주변 발급처 검색. **키 미확보로 `PLC-01-01~03` 이 막혀 있다**

---


# 실행 명령

## Backend

```bash
cd homerun-backend
cp .env.example .env      # POSTGRES_PASSWORD 는 반드시 채운다
npm install               # husky 훅 설치
./gradlew bootRun         # PostgreSQL 은 spring-boot-docker-compose 가 알아서 띄운다
```

`docker compose up` 을 따로 할 필요가 없다.

| 명령 | 하는 일 |
|---|---|
| `./gradlew build` | 컴파일 + spotlessCheck + test. CI 및 pre-push 와 같다 |
| `./gradlew test` | 테스트만. Docker 가 떠 있어야 한다 |
| `./gradlew spotlessApply` | 포맷 검사에 걸렸을 때 고치는 명령 |
| `npm run commit` | 커밋 타입을 골라 컨벤션에 맞는 메시지로 커밋 |
| `docker compose -f compose.prod.yaml up -d --build` | 앱까지 컨테이너로 |

| 확인 | 주소 |
|---|---|
| API 문서 | `http://localhost:8080/swagger-ui.html` |
| 헬스체크 | `http://localhost:8080/actuator/health` |

## Frontend

```bash
cd homerun-frontend
cp .env.example .env      # NUXT_PUBLIC_API_BASE=http://localhost:8080
pnpm install
pnpm dev
```

| 명령 | 하는 일 |
|---|---|
| `pnpm dev` | 개발 서버 |
| `pnpm lint` / `pnpm lint:fix` | ESLint |
| `pnpm typecheck` | vue-tsc 타입체크 |
| `pnpm verify` | lint + typecheck + build. 푸시 전 전체 검증 |
| `pnpm commit` | 컨벤션 커밋 |

## 팀원 로컬 초기 설정

```bash
cd homerun-backend  && npm install
cd homerun-frontend && pnpm install
```

`husky` 는 `prepare` 스크립트에서 자동 설치된다.

---


# Git 규칙

## 브랜치 흐름

```text
main
  └─ dev
      ├─ feat/#이슈번호-기능명
      ├─ fix/#이슈번호-버그명
      └─ chore/대상
```

* **기본 브랜치는 `dev` 다.** `develop` 이 아니다.
* `dev` · `main` 직접 push 는 하지 않는다. `dev` 로 PR 을 낸다.
* `main` 병합은 배포 단위로 한다.

## 작업 하나마다 이슈와 브랜치

**순서를 지킨다 — 이슈 먼저, 그다음 브랜치, 그다음 코드.** 일을 끝내고 이슈를 사후에 파지 않는다.

한 브랜치에 여러 작업을 쌓지 않는다. 작업 중 다른 게 눈에 띄면 그 브랜치에서 이어 하지 말고 별도 이슈로 뺀다. **리팩터링은 특히 기능과 섞지 않는다** — `ErrorCode` 나 `SecurityConfig` 같은 공유 파일을 건드리므로 리뷰어가 따로 봐야 한다.

푸시한 뒤에는 PR 의 head SHA 를 확인한다. 이미 머지되어 닫힌 PR 의 브랜치에 밀어넣으면 아무 데도 안 붙고 CI 도 안 돈다.

## 커밋 메시지

commitlint 가 검사한다.

```text
{이모지} {Type}: 한글 설명
```

**허용 type 의 단일 출처는 `commitlint.config.js` 의 `type-enum` 이다.** 아래 표와 어긋나면 설정이 맞다.

| 커밋 타입 | 용도 |
|---|---|
| `✨ Feat` | 새로운 기능 |
| `🚨 Fix` | 버그 수정 |
| `📝 Docs` | 문서 수정 |
| `♻️ Refactor` | 리팩토링 |
| `✅ Test` / `🧪 Test` | 테스트 코드 |
| `📦 Chore` | 설정, 빌드, 기타 |
| `💄 Design` | 디자인 수정 |
| `🎨 Style` | 코드 스타일 |
| `🔒 Security` | 보안 |
| `🎉 Init` | 초기 설정 |
| `🚑 Hotfix` | 긴급 수정 |

```text
✨ Feat: 정책 자격 3단계 판정 API 구현
✅ Test: 정책 자격 3단계 판정 단위테스트 추가
🚨 Fix: 버팀목 순자산 경계값 비교 연산자 수정
```

* 제목 끝에 마침표를 찍지 않는다(`subject-full-stop`).
* 한 커밋에는 하나의 논리적 변경 단위를 담는다.
* 백엔드와 프론트엔드의 `commitlint.config.js` 는 같은 규칙이다. **한쪽을 고치면 다른 쪽도 같이 고친다.**

이슈 제목도 커밋과 같은 접두사를 쓴다.

**이슈 본문은 `.github/ISSUE_TEMPLATE/issue.yml` 폼을 따른다** — ✨ 작업 내용 / ⚙ 상세 작업 내용(체크리스트) / 🛠 예상 소요 시간 세 필드다. `gh issue create --body`로 만들면 이 폼을 그냥 지나쳐서 자유 형식으로 새 나가기 쉽다 — 세 필드를 직접 갖춰 써야 한다. 상세 작업 내용은 완료된 항목을 `[x]`로, 이번에 뺀 항목을 `[ ]`로 남겨서 사후에도 무엇을 왜 안 했는지 보이게 한다.

## Pull Request

`.github/pull_request_template.md` 를 채운다. 항목을 여기 옮겨 적지 않는다. **자유 형식 본문(예: `## 무엇을` / `## 설계` 같은 임의 섹션)은 아무리 내용이 충실해도 컨벤션 위반이다** — 템플릿의 여섯 섹션(관련 이슈 · 작업 내용 · 테스트 방법 · 스크린샷 · 체크리스트 · 리뷰 요청 사항) 제목을 그대로 쓰고 내용만 채운다.

**관련 이슈에는 `Closes #이슈번호`를 반드시 적는다.** 이게 있어야 PR 머지 시 이슈가 자동으로 닫힌다 — 빠뜨리면 이슈가 닫힌 PR 뒤에 계속 열린 채로 남는다.

**체크리스트는 정직하게 채운다.** 안 한 항목을 체크하지 않는다 — 특히 "API 명세 또는 문서를 수정했습니다"는 새 엔드포인트를 만들거나 응답 필드를 바꿨는데 노션 API 명세서를 안 고쳤으면 체크하지 않고 리뷰 요청 사항에 남긴다.

**리뷰 요청 사항은 비워 두지 않는다.** 판단이 갈린 곳, 남이 봐 줘야 하는 곳, 남의 영역을 건드린 곳을 적는다. 그게 없으면 리뷰가 형식이 된다.

## 팀 운영 규칙

* **15분 룰** — 15분 안에 안 풀리면 바로 공유한다.
* PR 은 작게 만든다. 리뷰 대기가 30분을 넘으면 셀프 머지한다.
* 리스크 레지스터 기준 **확률 × 영향 12점 이상은 방치하지 않는다.**
* 데일리 스탠드업은 아침·저녁 3줄.

---


# 자동 검사

훅과 CI가 무엇을 언제 돌리는지만 알면 된다. 나머지는 `.github/` 와 `.husky/` 를 보면 된다.

| 시점 | 실행 | 걸렸을 때 |
|---|---|---|
| `commit-msg` | commitlint | 메시지 형식을 고친다. 본문 한 줄은 **100자 이내** |
| `pre-commit` | `./gradlew spotlessCheck` | `./gradlew spotlessApply` |
| `pre-push` | `./gradlew build` | 원인별 |
| CI (`ci.yml`) | `./gradlew build` | 위와 같다 |

**`pre-push` 와 CI 는 같은 태스크다. 여기서 통과하면 CI도 통과한다.** Docker가 떠 있어야 한다.

주의할 것 두 가지.

* **CI 는 머지 결과를 검사한다.** 내 브랜치가 초록불이어도 `dev` 가 그 뒤에 움직였다면 그 초록불은 낡은 것이다. 머지 전에 리베이스하고 체크가 **새로 돌아** 초록불인지 보고 누른다.
* `cd.yml` 의 `deploy` 잡은 비어 있다. 배포 서버가 정해지지 않았다.

---


# LLM 작업 원칙

## 무엇을 먼저 읽는가

1. 현재 브랜치와 변경분 (`git status --short`, `git diff`)
2. 작업과 관련된 **기존 코드**. 같은 일을 하는 도메인이 이미 있는지 본다
3. 기능ID 가 관련되면 **시트2** 에서 요구사항 원문
4. 기준 수치가 필요하면 **`config_effective`**

## 이 프로젝트에서 특히 조심할 것

* **정책 수치를 코드에 하드코딩하지 않는다.** 전부 `config_effective` 에서 읽는다.
* **팩트를 읽을 때 그게 무엇의 값인지 확인한다.** 같은 서류도 열람과 발급이 값이 다르다. 항목 이름이 모호하면 그대로 쓰지 말고 갈라 놓는다.
* **판정 결과를 LLM 이 만들게 하지 않는다.** LLM 은 이미 나온 판정을 설명만 한다.
* **소득 금액, 가족관계 상세값을 저장하는 컬럼을 만들지 않는다.** 충족 여부 boolean 만 남긴다.
* **`D-21`, `심사 2~3주` 같은 준비기간을 법정 기한처럼 다루지 않는다.** 위험이 크다는 것과 법이 정했다는 것은 다르다.
* **모르는 것을 통과로 보지 않는다.** 근거 없는 기본값을 채우는 것이 곧 틀린 안내다.
* 요구사항에 없는 기능을 임의로 추가하지 않는다. 필요하다고 보이면 시트2에 없다는 사실과 함께 제안한다.
* 구현 여부를 확인하지 않은 기능을 완료된 것으로 문서화하지 않는다.

## 하지 않는 것

* 사용자 요청 없이 브랜치를 전환하거나 변경분을 stash · reset · checkout 하지 않는다.
* 비밀값·토큰·API 키를 코드나 문서에 넣지 않는다. `.env` 에는 예시만 둔다.
* 요청 범위를 벗어난 대규모 리팩터링을 하지 않는다.
* 없는 라이브러리를 임의로 추가하지 않는다. 이유와 대안을 먼저 말한다.

---


# Backend 작업 기준

## 기본 구조

패키지 루트는 `com.homerun` 이다. 구조는 이미 정해져 있다(`#24`). 새로 지어내지 않는다.

```text
domain/{기능}/
  controller  service  repository  entity
  dto/        요청·응답. 커지면 dto/request · dto/response 로 나눈다
  type/       enum. DB CHECK 제약과 값이 같아야 한다
  model/      DB 에 없는 계산용 값 객체
  handler/    도메인 전용 예외 처리

global/
  config  response  exception
  security/{config,filter,handler,jwt,principal}
  external/{address,building,mail,openbanking,realestate}
```

* MyBatis 가 아니라 **Spring Data JPA** 를 쓴다. Mapper XML 은 없다.
* **`type/` 의 enum 은 DB CHECK 제약과 짝이다.** 한쪽만 고치면 런타임에 터진다.
* 외부 API 호출은 `global/external` 아래 클라이언트로 감싼다. 서비스가 직접 `RestClient` 를 들지 않는다.

## 공통 응답

* 성공 응답은 `global.response.ApiResponse<T>` 로 감싼다. `ApiResponse.success(data)` 를 쓰고 **새 래퍼를 만들지 않는다.**
* HTTP Status, DTO 반환, Validation 오류 처리 방식은 기존 Controller를 보고 맞춘다.


## 예외 처리

* `BusinessException` + `GlobalExceptionHandler` + ErrorCode 열거형 구조를 기본으로 한다.
* 예외를 추가할 때는 기존 예외 계층과 에러 응답 형식을 유지한다.
* Controller에서 광범위한 `try-catch` 로 예외를 삼키지 않는다.
* API 명세서에 이미 정의된 에러 코드를 그대로 쓴다. 예: `CONSENT_TOKEN_INVALID`, `CONSENT_TOKEN_EXPIRED`, `RULE_VERSION_MISMATCH`(409)

## 인증 및 인가

* 세션 토큰 / 동의 토큰 / 운영자 / 없음 네 종류가 있다.
* **동의 토큰은 가구원용 일회용 토큰**이다. 경로 파라미터 방식, 해시 저장, 72시간 만료, 로그 마스킹, `Referrer-Policy: no-referrer` 를 지킨다.
* 권한별로 조회·수정 가능한 데이터와 API를 분리한다(`SEC-01-04`).
* Spring Security 가 붙어 있다. `global.security` 아래에 `config` · `filter` · `handler` · `jwt` · `principal` 이 있다. 컨트롤러는 `@AuthenticationPrincipal MemberPrincipal` 로 회원을 받는다.
* **남의 계획을 건드리지 못하게 막는 것은 서비스의 일이다.** `plan.verifyOwner(memberId)` 를 통과하지 않고 계획 하위 자원을 읽거나 쓰지 않는다(`SEC-01-04`).

## JPA

* Entity 필드는 실제 컬럼명·`nullable`·길이 제약과 맞춘다.
* `open-in-view: false` 다. **지연 로딩 접근은 트랜잭션 안에서 끝낸다.**
* **Entity 를 바꾸면 마이그레이션을 함께 추가한다.** `ddl-auto: validate` 라서 어긋나면 앱이 부팅되지 않는다.

## 데이터베이스 변경

**스키마 변경은 전부 Flyway 마이그레이션으로 한다.** `V{버전}__{소문자_snake_case}.sql`

* **이미 머지된 파일은 절대 수정하지 않는다.** Flyway 가 체크섬을 저장하므로 내용이 바뀌면 다음 실행에서 실패한다. 고칠 게 있으면 새 버전을 추가한다.
* **번호는 브랜치마다 겹친다.** 여러 사람이 동시에 작업하면 거의 반드시 부딪힌다. PR 전에 `dev` 를 받아 최신 번호를 확인하고, 겹치면 내 파일 번호를 올린다. `MigrationFilenameTest` 가 중복을 빌드에서 잡는다.
* 스키마를 바꿨으면 Entity · DTO · Repository 쿼리 · 테스트를 함께 본다. `MigrationTest` 의 테이블 수와 팩트 수 단언도 같이 고쳐야 한다.


## 스키마 개요

**52 테이블.** `V1` 이 47개를 만들고 이후 마이그레이션이 다섯 개를 더했다.

| 도메인 | 테이블 |
|---|---|
| 공통·사용자 | `region` `terms` `app_user` `user_agreement` `refresh_token` |
| 금융연동 | `bank_connection` `financial_snapshot` `open_banking_connection` |
| 계획·진단 | `plan` `plan_input` `plan_input_history` `cost_estimate` `diagnosis` |
| 정책·판정 | `guarantee_agency` `policy` `policy_rule` `policy_snapshot` `policy_exclusion_group` `policy_exclusion_member` `policy_verdict` `verdict_basis` `rejection_reason` |
| 대안·자산 | `alternative_plan` `asset_option` |
| 매물·계약 | `property` `property_check` `lease_contract` `contract_special_term` |
| 서류·증빙 | `document_type` `document_issue_method` `user_document` |
| 가구원·동의 | `household_member` `consent_token` |
| 순서·데드라인·신청 | `plan_step` `step_task` `deadline` `application` |
| 홈·사후관리 | `loan_account` `fixed_expense` `monthly_checkin` `delinquency_risk` |
| 교육 | `education_content` `education_progress` `education_trigger` `intervention_log` |
| AI 코치 | `knowledge_card` `coach_session` `coach_message` `ai_generation` |
| 운영·설정 | `config_effective` `external_api_health` `audit_log` |

설계 시 알아 둘 것.

* **전세·월세·반전세는 같은 테이블을 쓴다.** `lease_type` 으로만 분기한다. 월세용 테이블을 따로 만들지 않는다.
* **`plan_step` 과 `step_task` 는 층이 다르다.** `plan_step` 은 관문 5개(온보딩·진단·정책선택·실행·정착)고, 사용자가 실제로 하는 일은 `step_task` 다. 마감(`deadline`)은 관문이 아니라 할 일에 붙는다.
* **교육 콘텐츠를 직접 만들지 않는다.** `education_content` 는 심의된 외부 자산 참조다.
* **`knowledge_card` 에는 사람이 검수한 카드만 들어간다.** `fact_refs` 가 `config_effective.fact_code` 를 가리킨다. AI가 말한 수치가 팩트 레지스트리에 없으면 그 답을 내보내지 않는다.
* **검색은 벡터가 아니라 태그다.** `trigger_codes` / `topic` 에 GIN 인덱스가 있다. 사용자가 보고 있는 화면과 판정 결과가 가장 좋은 검색 신호다.
* 판정 결과에는 규칙 버전 FK를 박아 `RULE_VERSION_MISMATCH` 비교가 가능해야 한다.

ERD: `https://dbdiagram.io/d/6a96771f5450bea1beb84886`


## Spring 환경 주의사항

현재 백엔드는 다음 환경을 사용한다.

* Spring Boot 4.1.1
* Java 17
* `jakarta.*`
* PostgreSQL 17

사용자의 명시적 요청 없이 다음 변경을 하지 않는다.

* Spring Boot 버전 변경
* JPA에서 MyBatis로 전환하거나 그 반대
* `ddl-auto` 를 `validate` 이외의 값으로 변경
* H2 등 인메모리 DB를 테스트에 도입 (Postgres 전용 문법이 검증되지 않는다)
* Testcontainers 제거

---

# Frontend 작업 기준

프론트엔드 규칙은 `homerun-frontend` 저장소의 같은 문서를 따른다. 이 저장소에서 화면 코드를 만들지 않는다.

한 가지만 여기 남긴다. **백엔드가 판정을 내리고 프론트는 그것을 보여 준다.** 화면에서 금액·자격을 다시 계산하지 않는다. 같은 수치를 두 곳에서 계산하면 반드시 갈라진다.

---


# 도메인 규칙 — 절대 어기면 안 되는 것

기능이 아니라 설계 전제다. 하나라도 깨지면 서비스가 성립하지 않는다.

## 판정은 규칙 엔진, AI는 설명만

* 정책 자격 판정은 `policy_rule` 의 버전 있는 JSON 조건식으로만 나온다.
* LLM은 이미 나온 판정을 쉬운 말로 바꿔 말할 뿐 결과를 바꾸지 못한다(`AI-02-03`).
* 같은 규칙 버전 + 같은 입력이면 항상 같은 결과다(`NFR-01-01`).
* LLM 호출이 실패해도 판정과 기본 템플릿 설명은 정상 제공한다(`AI-01-04`).

역할 분담은 **규칙 엔진 = 판정·순서·데드라인 / LLM = 설명·문구·조건식 초안 / 사람 = 조건식 최종 확정** 이다.

## 근거 없는 안내를 하지 않는다

* AI가 말한 정책 조건·서류·발급방법에는 반드시 공식 원문 링크가 붙는다(`AI-01-03`, `AI-02-04`).
* 투자 권유와 개별 금융상품 추천은 하지 않는다(`AI-02-05`).

## Zero-retention

* 소득·가족관계 증빙의 상세값은 판정 후 버린다. `verdict_basis` 에는 `is_met BOOLEAN` 만 남는다. **금액 컬럼 자체가 없어야 한다**(`SEC-01-01`).
* 가구원은 이름·주민식별정보를 저장하지 않고 관계와 확인 여부만 관리한다(`SEC-01-05`).
* 문서 원문·주민식별정보·상세 소득값은 애플리케이션 로그에도 남기지 않는다(`SEC-01-02`).

## 기한 세 종류를 섞지 않는다

`deadline.deadline_type` 은 셋 중 하나이고 화면에서도 구분한다(`SEQ-01-06`).

| 값 | 뜻 | 예 |
|---|---|---|
| `LEGAL` | 법정·공식 신청기한 | 잔금일·전입일 중 빠른 날부터 3개월 |
| `RECOMMENDED` | 서비스 권장 착수일 | 잔금일 D-21 착수, 서류 D-7 발급 |
| `PROCESSING` | 기관 처리 예상기간 | 대출 심사 2~3주 |

`D-21`, `15일 전`, `D-7`, `심사 2~3주` 같은 숫자를 **Rule Engine에 공식 규칙으로 넣지 않는다.** 은행·보증기관·상품마다 다르다. 전부 `RECOMMENDED` 다. 법정 기한을 놓치면 정책자금이 영구히 날아가고 권장일은 놓쳐도 만회된다. 같은 톤으로 표시하면 사용자가 진짜 마감을 알아보지 못한다.

## 확정 안 된 수치로 판정하지 않는다

* 기준 수치가 확정되지 않은 정책은 가능/불가로 판정하지 않고 **추가 확인** 상태로 둔다(`NFR-01-06`).
* `config_effective.confidence` 가 `CONFLICT` / `UNKNOWN` 이면 판정하지 않는다.
* 모든 입력 항목은 **모름을 허용**한다. 모름은 불가가 아니라 추가확인이다(`COM-05-04`).

## 범위 제외

| 코드 | 제외 | 대신 |
|---|---|---|
| `OUT-01-01` | 마이데이터 계좌·자산 자동연동 | 수기 입력 + 오픈뱅킹 목 데이터 |
| `OUT-01-02` | 정부 서류 자동발급 | 공식 발급처 딥링크 |
| `OUT-01-03` | 정책 신청서 실제 기관 제출 | 준비·이동·상태 관리까지 |
| `OUT-01-04` | 실제 대출 실행, 상품 가입 | 비교·안내까지 |

"연결까지"가 경계다. 데모에서 이 선을 넘는 화면을 만들지 않는다.

**회원가입은 범위 제외가 아니다.** 노션 개요에 남은 "범위 제외: 회원가입" 은 낡은 문장이다.

---

# 도메인 지식 — 모르고 짜면 틀린 걸 만든다

노션 **플로우 정의** 에서 뽑았다. 판정·화면 로직을 짜기 전에 읽는다.

## 대출 심사와 보증 심사는 다르다

전세대출은 **은행 심사 + 보증기관 심사 두 단계를 모두** 통과해야 한다. 거절 시 어디서 막혔는지 구분해야 하고, 대처가 완전히 다르다(`APP-01-07`, `GTE-01-01`).

| 보증기관 | 한도 산정 기준 | 반환보증 |
|---|---|---|
| HF 한국주택금융공사 | **사람** — 차주의 소득·신용등급 | HF 전세대출 이용자만 |
| HUG 주택도시보증공사 | **집** — 집의 가치와 안전성 | 대출보증 + 반환보증 결합형, 1순위 추천 |
| SGI 서울보증 | — | 2순위 추천 |

## 순서를 틀리면 되돌릴 수 없다

```text
은행 사전상담 → 매물 검증 → 계약 → 확정일자 → 대출신청 → 잔금·전입 → 반환보증
```

* **공인중개사보다 은행이 먼저다.** 등기부 떼서 2~3곳 상담. 계약을 다 하고 가면 거절될 수 있다(`PRP-02-01`).
* 원칙상 이사갈 집 근처 은행에서만 대출이 된다. 모든 영업점이 취급하지도 않는다.
* 기금대출을 받을 거면 중개사에게 먼저 말한다.
* **잔금 · 전입신고 · 확정일자는 같은 날 처리한다**(`PRP-02-05`, `RF-MV-01-01`).

## 대항력 — 자주 틀리는 부분

* **확정일자만 빨리 받는다고 대항력이 생기지 않는다.**
* 대항요건 = **주택 인도(실제 점유) + 전입신고**. 확정일자는 **우선변제권**이다. 서로 다른 제도다.
* 대항력은 전입신고 **다음날 0시**부터 발생한다. 그 사이 임대인이 근저당을 설정하면 은행이 선순위가 된다(`RF-MV-01-02`).

권장 특약 3종.

1. 잔금일 다음날까지 근저당 설정·전입세대 변동 금지
2. 대출 미승인 시 계약 무효, 계약금 전액 반환
3. 반환보증 가입 불가 시 계약 무효

**"필수 특약"이라고 단정하지 않는다.** 법적 강제 조항으로 오해된다. **"대출 이용 예정자에게 권장하는 특약"** 으로 표기한다(`PRP-02-03`).

## 매물 검증 — 하나라도 걸리면 막힌다

| 확인 | 걸리면 |
|---|---|
| 등기부 — 소유자 일치, 근저당, 선순위채권 | 신탁등기면 위험 |
| 건축물대장 — 위반건축물 | **근생빌라는 대출·보증 둘 다 거절** |
| 공시가격 | 반환보증 가입 가능 = **보증금 ≤ 공시가격 × 1.26** (140% × 전세가율 90%) |
| 전입세대확인서 | 단독·다가구는 필수. **온라인 발급 불가**, 주민센터 방문만 |
| 다가구 여부 | 전세대출 자체가 안 되는 경우가 많다 |
| 전세가율 | 80% 초과면 깡통전세 가능성(`PRP-01-05`) |

## 월세 — 전세와 다른 부분

* **보증부월세** = 보증금 대출 + 월세금 대출 **두 부분**. 서비스에서 분리한다. 보증금 4,500만(1.3%) + 월세 50만, **월 20만원까지 연 0%**, 초과분 1.0%.
* **주거안정월세대출** = 월세자금만. 월 60만, 우대형 1.3% / 일반형 1.8%. 잔금일에 한 번에 받는 게 아니라 계약기간 동안 매월 지급된다.
* 사회초년생은 주거안정 우대형 대상이기도 해서 **둘 다 판정**한다.
* **중복 불가 조합**(`POL-04-07`)
  * 주거급여 수급 → 청년월세 특별지원 제외
  * 국토부 청년월세 ↔ 지자체 월세지원 동시 불가
* **월세 세액공제**(`POL-04-05`, `POL-04-06`)
  * 총급여 5,500만 이하 17% / 7,000만 이하 15%, 연 750만 한도(최대 127.5만)
  * 전입신고를 해야 받는다
  * **지원금 받은 월세분은 공제 대상에서 빠진다.** 순서는 ① 지원금 차감 → ② 남은 본인부담에 공제율. 따로 더하면 과대계산된다
  * 전세와 월세를 비교할 때 이걸 빼면 월세가 부당하게 손해로 보인다
* **소액임차인 최우선변제**(`PRP-01-07`) — 서울 보증금 1.65억 이하면 최대 5,500만. 대항력이 있어야 하고, 판단 시점이 계약일이 아니라 **경매개시결정 등기일** 기준이다.
* **청년 주거급여 분리지급**(`FAM-01-05`) — 만 19~30세 미혼, 부모와 시·군이 달라야 한다. **부모 가구가 이미 주거급여를 받고 있어야 한다.** 청년 혼자서는 신청이 안 된다. 서울 1인 기준임대료 상한 36.9만원(2026).

## 반전세

실무에서 제일 흔한데 전세/월세로만 나누면 통째로 빠진다(`POL-05`).

```text
전월세전환율 = (월세 × 12) ÷ (전세금 − 전환후 보증금) × 100
법정 상한 = 기준금리 + 2% → 2026년 4.5%
```

**이 상한은 갱신할 때만 적용된다. 신규 계약은 제한이 없다.** 청년 첫 집은 대부분 신규계약이라 4.5%를 기준처럼 안내하면 안 된다. 시장 전환율과 비교해 "이 반전세가 비싼가 싼가"를 보여주는 용도로 쓴다(`POL-05-03`).

## 자산 — `AST-01` 이 존재하는 이유

* **IRP를 전세보증금으로 인출하면 16.5% 기타소득세가 그대로 붙는다.** 법정 인출 사유이긴 하나 세법상 '부득이한 사유'가 아니다.
* 주택청약종합저축은 5년 이내 해지 시 소득공제를 추징당한다.
* "노후자산을 깨서 보증금을 만든다"는 선택은 계산해서 금액으로 보여줘야 한다.

## 판정 기준값

* ~~RIR 20% 이하 안정 / 30% 초과 위험~~ — **판정에 쓰지 않는다.** RIR 자체를 범위에서 뺐다(아래 **RIR 제외 결정**)
* 버팀목 대상 주택: 임차보증금 3억 이하, 전용 85㎡ 이하(주거형 오피스텔 가능)
* 버팀목 우대: 만 25세 미만 단독세대주(전용 60㎡ 이하 · 보증금 3억 이하 · 대출 1.2억 이하) 연 0.3%p, 최대 4년
* 보증료 지원 소득 기준: 청년(19~39세) 5,000만 / 신혼(혼인 7년 내) 부부합산 7,500만 / 일반 6,000만

---

# 기준 수치 — 팩트 레지스트리

`V2__seed_config_effective.sql` 에 141행이 들어간다. 금액·한도·비율은 전부 여기서 읽는다.

* **금액은 전부 원(₩) 단위로 정규화돼 있다.** `3억원` 은 `300000000` 이다. `value_num` 을 쓸 때 `unit` 을 같이 확인한다.
* 매년 바뀌는 수치는 `effective_from` / `effective_to` 로 관리한다(`ADM-01-07`).

| confidence | 건수 | 쓰는 법 |
|---|---|---|
| `CONFIRMED` | 122 | 그대로 판정에 쓴다 |
| `REVIEW` | 8 | 쓰되 화면에 "변경 가능" 표시 |
| `UNKNOWN` | 5 | **판정 금지.** 추가 확인 상태로 |
| `CONFLICT` | 2 | **판정 금지.** 아래 참고 |
| `CORRECTED` | 2 | 정정된 값. 구 문서를 믿지 않는다 |
| `RETIRED` | 2 | 폐지. 대체 상품을 함께 안내 |

## 개발 전에 반드시 확정해야 하는 것

| 코드 | 항목 | 충돌 |
|---|---|---|
| `FCT-004` | 버팀목 순자산 기준 | **3.37억 ↔ 3.45억** |
| `FCT-009` | 청년 버팀목 대출한도 | **1.5억 ↔ 2억** (2025.6 축소, 계약 체결일 기준으로 갈림) |

**이 둘이 정해지기 전에는 버팀목 판정 로직을 확정할 수 없다.** 주택도시기금 연도별 고시로 한쪽에 고정한다.

## 조사하면서 바로잡은 값

팀 문서에 아직 틀린 값이 남아 있다. 문서를 그대로 믿지 않는다.

| 코드 | 팀 문서 | 실제 |
|---|---|---|
| `FCT-026` | 보증부월세 월세분 금리 "연 1.0%" | **월 20만원까지 연 0%**, 초과분 연 1.0% |
| `FCT-040` | 서울 기준임대료 1인 34만원 | **369,000원** (2026 중위소득 6.51% 인상 반영) |
| `FCT-134` | 온통청년 API | **폐기(404).** 대안은 보조금24 / 공공데이터포털 / 복지로 |
| `FCT-135` | 중기청 대출 | **2025.1 신규 취급 중단.** `DISCONTINUED` 로 두고 청년 버팀목을 안내 |

---

# 요구사항과 기능ID

## 기능ID 체계

**시트2의 `세부ID` 가 프로젝트 전체의 단일 기능 식별자다.** API 명세서, 커밋, PR, 테스트 이름에 전부 이것을 쓴다.

```text
COM-01-01     블록-일련번호
RF-HM-05-02   홈(RF) 대시보드계열(HM) 5번블록 2번항목
```

문서에 `MEM-01`, `STATE-03`, `MATCH-07`, `JEONSE-23`, `MONTH-16` 이 보이면 **시트5 체계**다. 시트5도 살아 있는 문서지만 **코드와 API는 시트2를 쓴다.**

## 스프레드시트 탭 구성

요구사항 정의 스프레드시트에 탭이 5개 있고 각각 다른 문서다.

| 탭 | 행 | 정체 | 쓸 것인가 |
|---|---|---|---|
| 시트1 | 101 | 구버전. `COM-01 임시 세션` = 회원가입이 없던 시절 | 참고만 |
| **시트2** | **322** | **현재 통일본.** 기능 도메인 축 | **기준** |
| 팩트 레지스트리 | 141 | 기준 수치 원본. `V2` 로 이관됨 | 사용 |
| 시트4 | 62 | 요구사항 정의서. 시트2 ID 사용, **근거 출처 포함** | 근거 참조용 |
| 시트5 | 202 | 요구사항 정의서. MEM/STATE 체계, 주거유형(전세·월세·반전세) 축 | 화면 설계 참고 |

## API 명세서

노션 API 명세서에 66건이 정의돼 있고 전부 시트2 기능ID로 매핑돼 있다. 베이스 경로는 `/api/v1` 이다.

| 영역 | 대표 엔드포인트 |
|---|---|
| 세션·계획 | `POST /sessions` · `GET·DELETE /sessions/me` · `POST /plans/reset` · `GET /plans/progress` |
| 1루 | `GET·POST /profile` · `POST·GET /diagnosis` · `POST /diagnosis/simulate` |
| 2루 | `POST /policies/evaluate` · `GET /policies/{id}/evaluation` · `GET /policies/candidates` · `GET /eligibility/expiry` |
| 대안·자산 | `GET /alternatives/causes` · `POST /alternatives/recalculate` · `/assets/irp*` |
| 3루 | `/evidence*` · `/consents*` · `POST /contracts/risk-check` · `/plans/steps` · `/plans/deadlines` · `/applications*` |
| 홈 | `/settlement/*` · `/credit/*` |
| AI | `POST /ai/explain` · `POST /ai/messages/consent` |
| 운영 | `/admin/policies*` · `/admin/funnel` · `/admin/audit/evaluations` · `GET /health` |

새 API를 만들기 전에 노션 API 명세서에 이미 정의돼 있는지 확인한다. 정의를 바꿨으면 노션도 함께 고친다.

---

# 팀

## 파트 배정 (노션 팀원 역할 분담)

| 이름 | 역할 | 파트 | 주요 업무 |
|---|---|---|---|
| 김유환 | **팀장** | 프론트엔드 | 전체 일정·기능 총괄, 핵심 플로우 설계, 진단·매칭 시각화, 신청·완주 UI |
| 강태훈 | 팀원 | 프론트엔드 | 재무 진단 UI, 지원 전후 비교 화면, 부족자금 시뮬레이션 시각화 |
| 조수연 | 팀원 | 프론트엔드 | 대화형 AI 코치 UI/UX, 금융상품·대안 비교 UI, 신청 진행상태·금융관리 화면 |
| 최윤호 | 팀원 | 백엔드 | 도메인 모델·규칙 엔진, 정책 자격 판정·매칭, 조건식 관리, 공공 API 연동 |
| 박우진 | 팀원 | 백엔드 | DB 설계, 정책 수집·정규화, 증빙 그래프, 데드라인·완주 상태 관리 |

## 실제 백엔드 인원 (2026-09-04 갱신, 강태훈 확인)

**지금 백엔드를 진행하는 사람은 강태훈·김유환·최윤호 3명이다.** 위 노션 표(김유환·강태훈=프론트, 최윤호·박우진=백엔드 둘)는 낡았다. **박우진은 더 이상 백엔드를 진행하지 않는다.**

## 기능 배정 (시트2 담당자)

조수연 121건 · 박우진 66건 · 김유환 53건 · 최윤호 47건 · 강태훈 35건.

**두 배정이 서로 맞지 않고, 백엔드는 실제로 다르게 굴러가고 있다.** 위 표는 노션 기준이라 지금 누가 무엇을 만들고 있는지와 다르다. 아래 미해결 항목 참고.

---

# 문서 간 모순 및 미해결

작업 중 아래 항목과 마주치면 임의로 결정하지 말고 사용자에게 알린다.

## 파트 배정과 기능 배정 불일치

조수연은 노션 기준 **프론트엔드**인데, 시트2에서 맡은 121건에 `RF-HM-02-04 월 잔여자금 재계산`, `RF-AS-01-03 상환 대 저축 비교` 같은 **백엔드 계산 로직**이 다수 포함돼 있다. 백엔드는 지금 강태훈·김유환·최윤호 3명인데(2026-09-04 갱신, 박우진은 백엔드에서 빠짐) 홈 100건의 서버 로직이 누구에게도 잡혀 있지 않다.

→ 홈 블록을 **화면(FE) / 로직(BE)** 으로 쪼개 재배정이 필요하다. 현재 가장 큰 일정 리스크다.

백엔드 쪽도 표와 다르다. 시트2 담당자 열은 전세/월세 기준으로 매겨져 있는데 실제 작업은 그렇게 나뉘어 있지 않다. **시트2 담당자 열을 지금 상태에 맞게 갱신해야 한다.**

## 회원가입 범위

| 문서 | 내용 |
|---|---|
| 노션 홈런 개요 | "범위 제외: 회원가입(세션 임시 식별자)" |
| 시트1 (구버전) | `COM-01-01 임시 세션 생성` |
| **시트2 (현재)** | **카카오 소셜 로그인 · 이메일 로그인 · 회원 탈퇴** |
| 노션 플로우 정의 | 구글·카카오 로그인 + 이름·생년월일·휴대전화 인증·거주지·병역기간 |
| 시트5 | `MEM-01` 소셜 로그인 · `MEM-02` 회원정보 등록 |

→ **회원가입은 범위 안이다.** 노션 개요와 시트1이 낡았다.

## 리서치 미완료

| 항목 | 막히는 기능 |
|---|---|
| **전세가율 70/80% 컷오프 공식 출처** (`FCT-119`) | `PRP-01-05` · `RF-HM-06-02` |
| 청약저축 해지 손실 미조사 | `AST-01-05` |
| 가구원 동의가 필요한 정책 목록 (기술 설계는 완료) | `FAM-01-01`~`04` · `ALT-01-03` |
| KPI 측정 정의와 로그 스키마 | `KPI-01-01`~`05` |
| 정책 데이터 소스 — 보조금24가 실제로 열리는지 | `ADM-01-01`~`02` · `POL-02` |
| 1~3루 경쟁 서비스 조사 | 발표 차별화 |

**남은 임계값은 전세가율이다.** 70/80% 컷오프(`FCT-119`)는 아직 자체 기준이라, 화면에 그대로 등급으로 보여주면 심사에서 출처를 묻는 질문이 들어온다.

## RIR 제외 결정

**`DIA-02-03`(RIR 계산)과 `DIA-02-07`(RIR 판정 기준 적용)은 구현하지 않는다.** 2026-09-05 결정.

* 시트2에는 둘 다 **우선순위 높음**으로 남아 있다. 시트2를 아직 고치지 않았으므로 여기가 더 최신이다.
* `DIA-02-06`(독립 가능성 판정)은 시트2가 "자금, 현금흐름, **주거비 부담률**"을 기준으로 하라고 적었지만, 실제 구현은 **자금과 현금흐름 두 축**으로만 판정한다.
* 컷오프 팩트 `FCT-076`("안정 ~20% / 주의 20~30% / 위험 30% 초과")은 레지스트리에 있으나 출처가 **'자체 제안'** 이고 확정도가 `REVIEW` 다. 규칙상 못 쓰는 값은 아니지만(판정 금지는 `UNKNOWN`·`CONFLICT`), 발표에서 "왜 20%/30%인가"를 방어해야 한다는 부담이 있어 빼는 쪽을 택했다.
* `diagnosis.rir` · `rir_level` 컬럼과 `ck_rir_level` 제약은 **지우지 않고 비워 둔다**(`docs/architecture/dia-02-diagnosis.md`). 나중에 되살릴 때 마이그레이션 없이 채우면 된다.
* ⚠️ `FCT-077`(청년층 RIR 58%)은 확정도가 `UNKNOWN` 이고 비고에 **"발표에 쓰지 말 것"** 이라고 적혀 있다. 되살리더라도 이 값은 쓰지 않는다.

되살리기로 하면 `FCT-073`(전체 임차가구 중위수 15.8%)·`FCT-075`(국제 과부담 기준 30%)가 둘 다 `CONFIRMED` 라, 등급을 매기지 않고 **비교 기준으로 나란히 보여주는 방식**이 가장 방어하기 쉽다.

## 낡거나 비어 있는 문서

| 대상 | 문제 |
|---|---|
| 노션 홈런 개요 | `React` · `MySQL` 로 적혀 있음. 실제는 **Nuxt · PostgreSQL** 이다 (Redis 는 이메일 인증에 실제로 쓴다) |
| 노션 환경변수 모음집 | 빈 페이지 |
| 노션 회의록 DB | 비어 있음 |
| 노션 태스크 보드 | 12건 전부 `시작 전`, 담당자 전원 미배정 |
| 노션 테이블 정의서 `region` | 동시 편집 중 `parent_id` `rent_grade` `is_seoul` `is_overcrowd` 설명 유실. `V1__init.sql` 이 맞다 |
| API `/credit/debt-relief-paths` | 채무조정 요구사항이 시트2에 없음. `RF-CR-09-05` 추가 또는 API 삭제 |
| `cd.yml` | `deploy` 잡이 비어 있음. 배포 서버 미정 |
| 노션 API 명세서 | 구현된 경로와 어긋난 곳이 있다. 코드가 기준이다 |

---

# 링크

| 대상 | 주소 |
|---|---|
| GitHub org | `https://github.com/KB-hackathon-Rookies` |
| 요구사항 정의 (시트2) | `https://docs.google.com/spreadsheets/d/1Lr9AM9JRU17QwNwVcFtDZP0Q9u5G2OPlS0bX__t9NQE/edit?gid=702974646` |
| 노션 홈런 (루트) | `https://app.notion.com/p/3b81ddbd469b81769fbcdcb293c19f4c` |
| 플로우 정의 | `https://app.notion.com/p/3cd1ddbd469b80ada04ad6acf1899bf3` |
| API 명세서 | `https://app.notion.com/p/3c91ddbd469b80689f94c5480f7cfea1` |
| 테이블 정의서 | `https://app.notion.com/p/3cd1ddbd469b80b1942deb4c1f240217` |
| 리서치 허브 | `https://app.notion.com/p/3cd1ddbd469b81359de2d1f6df5aa54f` |
| 리서치 갭 | `https://app.notion.com/p/3cd1ddbd469b8194aa66e42d964092f7` |
| ERD | `https://dbdiagram.io/d/6a96771f5450bea1beb84886` |
