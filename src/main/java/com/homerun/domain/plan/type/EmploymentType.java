package com.homerun.domain.plan.type;

/** 고용 형태. 소득 안정성 판정과 일부 정책의 재직 요건에 쓰인다(DIA-01-04). */
public enum EmploymentType {
    FULL_TIME,
    CONTRACT,
    INTERN,
    DAILY_WORKER,
    FREELANCER,
    UNEMPLOYED
}
